<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Register your account in Uptask

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes) 
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                        
                    
                
                    
                

            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="Your Email" value=email) 
                .campo
                    label(for="passord") Password:
                    input(type="password" name="password" placeholder="Your Password" value=password )
                .campo.enviar
                    input.boton(type="submit" value="Create Account" )
                .campo
                    a(href="/iniciar-sesion") Log In
        