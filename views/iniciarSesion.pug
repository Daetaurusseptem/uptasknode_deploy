<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Login Uptask  

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes) 
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="Your Email" value=email) 
                .campo
                    label(for="passord") Password:
                    input(type="password" name="password" placeholder="Your Password" value=password )
                .campo.enviar
                    input.boton(type="submit" value="Log in" )
                .campo
                    a(href="/crear-cuenta") Register
                .campo
                    a(href="/restablecer") Did you forget your password?
        